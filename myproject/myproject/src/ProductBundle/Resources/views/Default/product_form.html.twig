{% extends '::base.html.twig' %}

{% block body %}
	{{ form_start(form) }}
		{{ form_widget(form) }}
		<input type="submit" value="Dodaj produkt" />
	{{ form_end(form) }}
	<a href=”{{ path('product_homepage') }}”>Anuluj</a>
{% endblock %}

